const arrayNumber = (0,1,2,3,4);

//É possível usar diferentes tipos de entrada no array

const MixeDeDados = (1, 'Array', false);

//Outra forma de implementar o array

const classArray = new Array(0, 1, 2, 3);

